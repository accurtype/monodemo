{
	"$schema": "https://json.schemastore.org/package",
	"private": true,
	"name": "accurtype",
	"description": "accurtype",
	"license": "MIT",
	"author": "accurtype",
	"repository": {
		"url": "git+https://github.com/accurtype/monodemo",
		"type": "git"
	},
	"type": "module",
	"scripts": {
		"prepare": "husky",
		"cz": "pnpm -s lint && cz",

		"prefmt": "prettier -w .",
		"fmt": "eslint . --fix",
		"prelint": "prettier -c .",
		"lint": "eslint .",

		"test": "pnpm run -r test",
		"build": "pnpm run -r build",
		"clear": "pnpm run -r clear",
		"ci": "pnpm lint && pnpm test && pnpm build && pnpm clear"
	},
	"devDependencies": {
		"husky": "^9.*.*",

		"@commitlint/cli": "^20.*.*",
		"commitizen": "^4.*.*",
		"cz-customizable": "^7.*.*",

		"eslint": "^9.*.*",
		"prettier": "latest",

		"rimraf": "latest",
		"make-dir-cli": "latest",
		"tsx": "latest",
		"esbuild": "latest",
		"typescript": "latest"
	},
	"config": {
		"cz-customizable": {
			"config": "./config/cz-config.cjs"
		},
		"commitizen": {
			"path": "./node_modules/cz-customizable"
		}
	},
	"prettier": {
		"printWidth": 200,
		"tabWidth": 4,
		"useTabs": true,
		"semi": true,
		"singleQuote": true,
		"jsxSingleQuote": false,
		"trailingComma": "all"
	}
}
